export class Upload {
    /**
     * 
     * @param API_key Token used for authentication when uploading songs or making a new playlist.
     */
    constructor(
        API_key: string = ""
    ) {

    }
}